# Simple-calculator-app-made-with-React
A simple calculator app using React. It performs basic math operations like add, subtract, multiply, and divide. Built with React functional components, useState for managing data, and event handling for buttons. Styled using CSS modules with a clean and responsive layout.
